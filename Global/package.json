{
  "name": "playwright-url-checker",
  "version": "1.0.0",
  "description": "A tool to check if specific text exists on web pages from a CSV list",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "csv-parse": "^5.5.3",
    "csv-stringify": "^6.4.5",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "p-limit": "^5.0.0",
    "playwright": "^1.42.1"
  }
}
